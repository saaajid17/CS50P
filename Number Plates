def main():
    plate = input("Plate: ")
    if is_valid(plate):
        print("Valid")
    else:
        print("Invalid")


def is_valid(s):
    first = bool(False)
    if len(s)> 6 or len(s)<2  :
        return False 
    elif s[0].isdigit()==True :
        return False
    elif s[1].isdigit()==True :
        return False
    elif s.isalnum()!=True :
        return False
    for letter in s :
        if letter.isdigit():
            if letter == "0" and first == False :
                first = True
                return False
            else :
                first = True
        elif letter.isalpha() == True and first== True :
            return False 
    return True    
main()
